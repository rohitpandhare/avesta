     <% if (locals.success || locals.error) { %>
    <div id="toast" role="alert" class="fixed top-4 right-4 flex items-center p-4 max-w-sm w-full space-x-4 text-gray-700 bg-white rounded shadow-lg transform transition-all duration-500">
        <div class="flex-shrink-0">
            <% if (locals.success) { %>
                <svg class="w-6 h-6 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
            <% } else if (locals.error) { %>
                <svg class="w-6 h-6 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            <% } %>
        </div>

        <div class="flex-1" id="topper">
            <% if (locals.success) { %>
                <p class="font-semibold text-green-600">Success!</p>
                <p class="text-sm"><%= success || 'Operation successful.' %></p>
            <% } else if (locals.error) { %>
                <p class="font-semibold text-red-600">Error!</p>
                <p class="text-sm"><%= error || 'An error occurred.' %></p>
            <% } %>
        </div>
        
        <button onclick="closeToast()" class="text-gray-500 hover:text-gray-700">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 
                  10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 
                  4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
            </svg>
        </button>
    </div>
<% } %>

   <div class="hamburger" id="hamburger">
        <div style="background-color: green;"></div>
        <div style="background-color: green;"></div>
        <div style="background-color: green;"></div>
    </div>

    <div class="flex h-screen">
        <aside class="sidebar flex flex-col h-full text-white" id="sidebar" style="background-color: #000030;">
            <span class="close-icon" id="close-icon">×</span>
            <div class="p-6 border-b border-gray-700">
                <h2 class="text-2xl font-bold text-white">Admin Panel</h2>
                <span class="text-white">Welcome Admin:
                    <span class="text-blue-600">
                        <%= user?.Username?.split('@')[0] || 'Admin' %>
                    </span>
                    <br>
                    <span class="text-white"> UserID: </span>
                    <span class="text-blue-600"><%= user?.UserID || '0' %></span>
                </span>
            </div>

            <nav class="p-4 flex-1 ">
                <ul class="space-y-2">
                    <li><a href="/admin" class="nav-item text-white hover:bg-gray-700"><i data-feather="home" class="w-5 h-5 mr-3"></i>Dashboard</a></li>
                    <li><a href="/admin/users" class="nav-item text-white hover:bg-gray-700"><i data-feather="users" class="w-5 h-5 mr-3"></i>Users</a></li>
                    
                    <li><a href="/admin/logs" class="nav-item text-white hover:bg-gray-700"><i data-feather="file-text" class="w-5 h-5 mr-3"></i>Logs</a></li>
                    <li><a href="/admin/reviveUser" class="nav-item text-white hover:bg-gray-700"><i data-feather="refresh-ccw" class="w-5 h-5 mr-3"></i>Revive User</a></li>
                </ul>
            </nav>

            <div class="p-4 border-t border-gray-700">
                <a href="/auth/logout" class="flex items-center px-4 py-2 text-red-500 hover:text-red-700 rounded-lg hover:bg-gray-700 transition-colors text-xl">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path></svg>
                    Logout
                </a>
            </div>
        </aside>

            
        <!-- Main Content -->
        <main class="main-content">
            <header class="flex justify-between items-center mb-8">
                <h1 class="text-2xl font-bold text-gray-800" style="margin-left: 130px;">Dashboard Overview</h1>
                <div class="flex items-center space-x-4">
                    <span class="text-gray-700">Welcome Admin,
                        <span class="text-blue-600">
                            <%= user?.Username?.split('@')[0] || 'Admin' %>
                        </span>
                    </span>
                </div>
            </header>
