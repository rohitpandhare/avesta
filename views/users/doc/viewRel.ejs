<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doctor Dashboard - Patients</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/users.css">
  <!-- <script src="[https://cdn.tailwindcss.com](https://cdn.tailwindcss.com)"></script> -->
  <!-- <style>
    /* Custom styles for sidebar toggle */
    @media (max-width: 768px) {
      #sidebar {
        transform: translateX(-100%);
        transition: transform 0.3s ease-in-out;
      }
      #sidebar.open {
        transform: translateX(0);
      }
      .hamburger {
        display: block;
        position: fixed;
        top: 20px;
        left: 20px;
        z-index: 1000;
        cursor: pointer;
      }
      .close-icon {
        display: block;
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 1.5rem;
        color: white;
        cursor: pointer;
      }
      #main-content {
        margin-left: 0 !important; /* Override inline style for mobile */
      }
    }
    @media (min-width: 769px) {
      .hamburger, .close-icon {
        display: none;
      }
      #sidebar {
        transform: translateX(0);
        position: relative; /* Ensure it takes up space in desktop view */
      }
      #main-content {
        margin-left: 256px; /* Adjust based on sidebar width */
      }
    }
  </style> -->
</head>

<body class="bg-gray-100 min-h-screen font-sans antialiased">
  <%- include('nav') -%>
 <div style="margin-left: 120px;">

      <h2 class="text-2xl font-semibold text-gray-800 mb-4">Patients</h2>
      <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">PatientName</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">First Consultation</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">DOB</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Phone</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Blood Group</th>
                <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Activity</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
              <% if (typeof doctorPatients !== 'undefined' && doctorPatients.length > 0) { %>
                <% doctorPatients.forEach(relationship => { %>
                  <tr>
                    <td class="px-6 py-4"><%= relationship.PatientName || 'N/A' %></td>
                    <td class="px-6 py-4"><%= relationship.FirstConsultation ? relationship.FirstConsultation.toLocaleDateString() : 'N/A' %></td>
                    <td class="px-6 py-4"><%= relationship.DOB ? relationship.DOB.toLocaleDateString() : 'N/A' %></td>
                    <td class="px-6 py-4"><%= relationship.Phone || 'N/A' %></td>
                    <td class="px-6 py-4"><%= relationship.BloodGroup || 'N/A' %></td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      <a href="/doctor/viewRelation/<%= relationship.PatientID %>" 
                         class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600">
                          View
                      </a>
                    </td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="6" class="px-6 py-4 text-center text-gray-500">No relationships found</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </main>
  </div>

  
 </div>
<script src="/javascripts/doctorHelp.js"></script>
</body>
</html>
