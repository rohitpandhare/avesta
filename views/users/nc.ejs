<!DOCTYPE html>
<html>
<head>
    <title>Doctor Dashboard</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <script src="https://cdn.tailwindcss.com"></script> -->
    <link rel="stylesheet" href="/css/styles.css">

</head>
<body class="bg-gray-100 min-h-screen">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 py-3">
            <div class="flex justify-between items-center">
                <div class="flex items-center">
                    <h1 class="text-2xl font-bold text-gray-800">Doctor Dashboard</h1>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="text-gray-700">
                        <span class="font-medium">Dr. <%= user?.Username %></span>
            
                    </div>
                    <a href="/auth/logout" class="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-red-600 transition-colors">
                        Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-4 py-4">
        <nav class="bg-white shadow-md rounded-lg">
            <ul class="flex justify-around p-4">
                <li>
                    <a href="#part1" 
                       class="flex items-center text-gray-700 hover:text-blue-600 hover:bg-blue-50 p-2 rounded-md transition-all duration-300"
                       onclick="scrollToSection('part1')">
                        <i data-feather="users" class="w-5 h-5 mr-2"></i>
                        Doctor-Patient Relations
                    </a>
                </li>
                <li>
                    <a href="#part2" 
                       class="flex items-center text-gray-700 hover:text-blue-600 hover:bg-blue-50 p-2 rounded-md transition-all duration-300"
                       onclick="scrollToSection('part2')">
                        <i data-feather="file-text" class="w-5 h-5 mr-2"></i>
                        Medical Records
                    </a>
                </li>
                <li>
                    <a href="#part3" 
                       class="flex items-center text-gray-700 hover:text-blue-600 hover:bg-blue-50 p-2 rounded-md transition-all duration-300"
                       onclick="scrollToSection('part3')">
                        <i data-feather="clipboard" class="w-5 h-5 mr-2"></i>
                        Prescriptions
                    </a>
                </li>
            </ul>
        </nav>
    </div>
        

    <!-- Main Content -->
<div class="max-w-7xl mx-auto px-4 py-8">
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            
            <!-- view current records  -->
    <div class="container mx-auto">

        <div id="part 1">            
            <!-- Doctor-Patient Relationships Table -->
            <div class="mb-8">
                <h2 class="text-2xl font-bold mb-4">Doctor-Patient Relationships</h2>
                <div class="overflow-x-auto bg-white rounded-lg shadow">
                    <table class="min-w-full">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-4 py-2 text-left">DoctorID</th>
                                <th class="px-4 py-2 text-left">PatientID</th>
                                <th class="px-4 py-2 text-left">FirstConsultation</th>
                                <th class="px-4 py-2 text-left">ConsultationType</th>
                                <th class="px-4 py-2 text-left">TreatmentNotes</th>
                            </tr>
                        </thead>
                        <tbody>
            <% if (typeof doctorPatients !== 'undefined' && doctorPatients.length > 0) { %>
                            <% doctorPatients.forEach(relationship => { %>
                                <tr class="border-t">
                                    <td class="px-4 py-2"><%= relationship.DoctorID %></td>
                                    <td class="px-4 py-2"><%= relationship.PatientID %></td>
                                    <td class="px-4 py-2"><%= relationship.FirstConsultation.toLocaleDateString() %></td>
                                    <td class="px-4 py-2"><%= relationship.ConsultationType %></td>
                                    <td class="px-4 py-2"><%= relationship.TreatmentNotes %></td>
                                </tr>
                            <% }) %>
                            <% } else { %>
                                <tr class="border-t">
                                    <td colspan="4" class="px-4 py-2 text-center text-gray-500">No doctorPatients found</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Doctor-Patient Relationship Form -->
            <!-- <section class="bg-white rounded-lg shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-6">Add Doctor-Patient Relationship</h2>
                
                <% if (locals.error) { %>
                    <div class="mb-4 p-3 bg-red-100 text-red-700 rounded-md"><%= error %></div>
                <% } %>
                <% if (locals.success) { %>
                    <div class="mb-4 p-3 bg-green-100 text-green-700 rounded-md"><%= success %></div>
                <% } %>

                <form action="/doctor/addPatient" method="POST" class="space-y-4">

                    <div class="hidden">
                        <label class="block text-sm font-medium text-gray-700 mb-1">Doctor ID</label>
                        <input type="number" 
                               name="currentDoctorID" 
                               readonly
                               value="<%= typeof currentDoctorID !== 'undefined' ? currentDoctorID : '' %>"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md bg-gray-50 text-gray-500">
                    </div>
                    

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Patient ID</label>
                        <input type="number" name="PatientID" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">First Consultation Date</label>
                        <input type="date" name="FirstConsultation" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Consultation Type</label>
                        <select name="ConsultationType" required 
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <option value="REGULAR">REGULAR</option>
                            <option value="EMERGENCY">EMERGENCY</option>
                            <option value="FOLLOWUP">FOLLOWUP</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Treatment Notes</label>
                        <textarea name="TreatmentNotes" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <button type="submit" 
                            class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                        Submit
                    </button>
                </form>
                <div class="mb-4">
                    <% if (locals.success) { %>
                        <div class="p-4 mb-4 text-sm text-green-700 bg-green-100 rounded-lg">
                            <%= success %>
                        </div>
                    <% } %>
                    
                    <% if (locals.error) { %>
                        <div class="p-4 mb-4 text-sm text-red-700 bg-red-100 rounded-lg">
                            <%= error %>
                        </div>
                    <% } %>
                </div>
                
            </section> -->
        </div>

        <div id="part2">
            <!-- Medical Records Table -->
            <div class="mb-8">
                <h2 class="text-2xl font-bold mb-4">Medical Records</h2>
                <div class="overflow-x-auto bg-white rounded-lg shadow">
                    <table class="min-w-full">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-4 py-2 text-left">RecordID</th>
                                <th class="px-4 py-2 text-left">PatientID</th>
                                <th class="px-4 py-2 text-left">DoctorID</th>
                                <th class="px-4 py-2 text-left">Diagnosis</th>
                                <th class="px-4 py-2 text-left">Symptoms</th>
                                <th class="px-4 py-2 text-left">Treatments</th>
                                <th class="px-4 py-2 text-left">RecordDate</th>
                                <th class="px-4 py-2 text-left">Notes</th>
                                <th class="px-4 py-2 text-left">UpdatedBy</th>
                            </tr>
                        </thead>
                        <tbody>
            <% if (typeof medicalRecords !== 'undefined' && medicalRecords.length > 0) { %>
                            <% medicalRecords.forEach(record => { %>
                                <tr class="border-t">
                                    <td class="px-4 py-2"><%= record.RecordID %></td>
                                    <td class="px-4 py-2"><%= record.PatientID %></td>
                                    <td class="px-4 py-2"><%= record.DoctorID %></td>
                                    <td class="px-4 py-2"><%= record.Diagnosis %></td>
                                    <td class="px-4 py-2"><%= record.Symptoms %></td>
                                    <td class="px-4 py-2"><%= record.Treatments %></td>
                                    <td class="px-4 py-2"><%= record.RecordDate.toLocaleDateString() %></td>
                                    <td class="px-4 py-2"><%= record.Notes %></td>
                                    <td class="px-4 py-2"><%= record.UpdatedBy %></td>
                                </tr>
                            <% }) %>
                            <% } else { %>
                                <tr class="border-t">
                                    <td colspan="4" class="px-4 py-2 text-center text-gray-500">No medicalRecords found</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

              <!-- Medical Records Form -->
              <!-- <section class="bg-white rounded-lg shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-6">Add Medical Record</h2>
                
                <form action="/test-medical-record" method="POST" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Patient ID</label>
                        <input type="number" name="PatientID" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div class="hidden">
                        <label class="block text-sm font-medium text-gray-700 mb-1">Doctor ID</label>
                        <input type="number" name="DoctorID" readonly 
                               value="<%= typeof doctorID !== 'undefined' ? doctorID : '' %>"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md bg-gray-50 text-gray-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Diagnosis</label>
                        <input type="text" name="Diagnosis" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Symptoms</label>
                        <textarea name="Symptoms" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Treatments</label>
                        <textarea name="Treatments" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Record Date</label>
                        <input type="date" name="RecordDate" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Notes</label>
                        <textarea name="Notes" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Updated By</label>
                        <select name="UpdatedBy" required 
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <option value="DOCTOR">DOCTOR</option>
                            <option value="STAFF">STAFF</option>
                        </select>
                    </div>

                    <button type="submit" 
                            class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                        Submit
                    </button>
                </form>
            </section> -->
        </div>

        <div id="part3">
                <!-- Prescriptions Table -->
                <div class="mb-8">
                    <h2 class="text-2xl font-bold mb-4">Prescriptions</h2>
                    <div class="overflow-x-auto bg-white rounded-lg shadow">
                        <table class="min-w-full">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-4 py-2 text-left">PrescriptionID</th>
                                    <th class="px-4 py-2 text-left">PatientID</th>
                                    <th class="px-4 py-2 text-left">DoctorID</th>
                                    <th class="px-4 py-2 text-left">DateIssued</th>
                                    <th class="px-4 py-2 text-left">DiagnosisNotes</th>
                                    <th class="px-4 py-2 text-left">Medicines</th>
                                    <th class="px-4 py-2 text-left">Status</th>
                                    <th class="px-4 py-2 text-left">GlobalReferenceID</th>
                                </tr>
                            </thead>
                            <tbody>
            <% if (typeof prescriptions !== 'undefined' && prescriptions.length > 0) { %>
                                <% prescriptions.forEach(prescription => { %>
                                    <tr class="border-t">
                                        <td class="px-4 py-2"><%= prescription.PrescriptionID %></td>
                                        <td class="px-4 py-2"><%= prescription.PatientID %></td>
                                        <td class="px-4 py-2"><%= prescription.DoctorID %></td>
                                        <td class="px-4 py-2"><%= prescription.DateIssued.toLocaleDateString() %></td>
                                        <td class="px-4 py-2"><%= prescription.DiagnosisNotes %></td>
                                        <td class="px-4 py-2 whitespace-pre-line"><%= prescription.Medicines %></td>
                                        <td class="px-4 py-2"><%= prescription.Status %></td>
                                        <td class="px-4 py-2"><%= prescription.GlobalReferenceID %></td>
                                    </tr>
                                <% }) %>
                                <% } else { %>
                                    <tr class="border-t">
                                        <td colspan="4" class="px-4 py-2 text-center text-gray-500">No prescriptions found</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>

                
            <!-- Prescription Form -->
            <section class="bg-white rounded-lg shadow-md p-6">
                <h2 class="text-xl font-semibold text-gray-800 mb-6">Add Prescription</h2>
                
                <form action="/doctor/addPres" method="POST" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Patient ID</label>
                        <input type="number" name="PatientID" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div >
                        <label class="block text-sm font-medium text-gray-700 mb-1">Doctor ID</label>
                        <input type="number" name="DoctorID" 
    
                               class="w-full px-3 py-2 border border-gray-300 rounded-md bg-gray-50 text-gray-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Date Issued</label>
                        <input type="date" name="DateIssued" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Diagnosis Notes</label>
                        <textarea name="DiagnosisNotes" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Medicines</label>
                        <textarea name="Medicines" required 
                                  class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                        <select name="Status" required 
                                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                            <option value="ACTIVE">ACTIVE</option>
                            <option value="COMPLETED">COMPLETED</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Global Reference ID</label>
                        <input type="text" name="GlobalReferenceID" required 
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    </div>

                    <button type="submit" 
                            class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                        Submit
                    </button>
                </form>
            </section>
        
        </div>
            
</div>
</div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
<script>
    // Initialize Feather Icons
    feather.replace();

    // Smooth scroll function
    function scrollToSection(sectionId) {
        const section = document.getElementById(sectionId);
        if (section) {
            section.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    }

    // Add active state to navigation
    document.addEventListener('DOMContentLoaded', function() {
        const navLinks = document.querySelectorAll('nav a');
        const sections = document.querySelectorAll('[id^="part"]');
        
        function setActiveLink() {
            let current = '';
            
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (window.pageYOffset >= (sectionTop - 150)) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('text-blue-600', 'bg-blue-50');
                if (link.getAttribute('href').slice(1) === current) {
                    link.classList.add('text-blue-600', 'bg-blue-50');
                }
            });
        }

        // Update active state on scroll
        window.addEventListener('scroll', setActiveLink);
        
        // Set initial active state
        setActiveLink();
    });
</script>

</body>
</html>
