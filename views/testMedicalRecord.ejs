<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Medical Record Entry</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
    <div class="max-w-md mx-auto bg-white p-6 rounded-lg shadow">
        <h1 class="text-2xl font-bold mb-6">Add Test Medical Record</h1>

        <% if (locals.error) { %>
            <div class="mb-4 text-red-600"><%= error %></div>
        <% } %>
        <% if (locals.success) { %>
            <div class="mb-4 text-green-600"><%= success %></div>
        <% } %>

        <form action="/test-medical-record" method="POST">
            <div class="space-y-4">
                <div>
                    <label class="block mb-1">Patient ID</label>
                    <input type="number" name="PatientID" required class="w-full border p-2 rounded">
                </div>

                <div>
                    <label class="block mb-1">Doctor ID</label>
                    <input type="number" name="DoctorID" required class="w-full border p-2 rounded">
                </div>

                <div>
                    <label class="block mb-1">Diagnosis</label>
                    <input type="text" name="Diagnosis" required class="w-full border p-2 rounded">
                </div>

                <div>
                    <label class="block mb-1">Symptoms</label>
                    <textarea name="Symptoms" required class="w-full border p-2 rounded"></textarea>
                </div>

                <div>
                    <label class="block mb-1">Treatments</label>
                    <textarea name="Treatments" required class="w-full border p-2 rounded"></textarea>
                </div>

                <div>
                    <label class="block mb-1">Record Date</label>
                    <input type="date" name="RecordDate" required class="w-full border p-2 rounded">
                </div>

                <div>
                    <label class="block mb-1">Notes</label>
                    <textarea name="Notes" required class="w-full border p-2 rounded"></textarea>
                </div>

                <div>
                    <label class="block mb-1">Updated By</label>
                    <select name="UpdatedBy" required class="w-full border p-2 rounded">
                        <option value="DOCTOR">DOCTOR</option>
                        <option value="STAFF">STAFF</option>
                    </select>
                </div>

                <button type="submit" class="w-full bg-blue-500 text-white p-2 rounded hover:bg-blue-600">
                    Submit
                </button>
            </div>
        </form>
    </div>
</body>
</html>
