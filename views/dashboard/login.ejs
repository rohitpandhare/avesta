<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - DoctorSync</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  
  <!-- Navbar -->
  <nav class="bg-white shadow-md w-full">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <a href="/" class="text-2xl font-bold text-blue-600 tracking-tight">DoctorSync</a>
        <ul class="flex space-x-6">
          <li><a href="/findDr" class="text-gray-600 hover:text-blue-600 font-medium transition">Find Doctors</a></li>
          <li><a href="/viewPres" class="text-gray-600 hover:text-blue-600 font-medium transition">View Prescription</a></li>
          <li><a href="/login" class="text-gray-600 hover:text-blue-600 font-medium transition">Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Centered login card -->
  <main class="flex-grow flex items-center justify-center px-4 py-12">
    <div class="w-full max-w-md bg-white rounded-2xl shadow-xl p-8 space-y-8">
      <!-- Header -->
      <div class="text-center">
        <h2 class="text-3xl font-bold text-gray-900">Welcome Back</h2>
        <p class="text-gray-600 mt-2">Login to your account</p>
      </div>

      <% if (locals.error) { %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= error %></span>
      </div>
      <% } %>

      <!-- Login Form -->
      <form action="/auth/login" method="post" class="space-y-6">
        <div>
          <label class="block text-sm font-medium text-gray-700">User Type</label>
          <div class="relative mt-1">
            <select name="Role" id="role" required 
                    class="w-full pl-12 pr-4 py-3 appearance-none border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 bg-white">
              <option value="PATIENT" selected>Patient</option>
              <option value="DOCTOR">Doctor</option>
              <!-- <option value="ADMIN">Admin</option> -->
            </select>
            <svg class="absolute left-4 top-3.5 w-5 h-5 text-gray-400" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700">Username</label>
          <div class="relative mt-1">
            <input type="text" name="Username" id="username" required 
                   class="w-full pl-12 pr-4 py-3 border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 placeholder-gray-400"
                   placeholder="Enter your username" />
            <svg class="absolute left-4 top-3.5 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
            </svg>
          </div>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-700">Password</label>
          <div class="relative mt-1">
            <input type="password" name="Password" id="password" required 
                   class="w-full pl-12 pr-4 py-3 border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 placeholder-gray-400"
                   placeholder="Enter your password" />
            <svg class="absolute left-4 top-3.5 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
            </svg>
          </div>
        </div>

        <button type="submit" 
                class="w-full bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-semibold py-4 rounded-xl transition-all shadow-md hover:shadow-lg">
          Access Portal
        </button>
      </form>

      <!-- Links -->
      <div class="text-center space-y-4">
        <div class="flex justify-center gap-4">
          <a href="/reset" class="text-sm text-blue-600 hover:text-blue-800 font-medium">Forgot Password?</a>
          <span class="text-gray-300">|</span>
          <a href="/signup" class="text-sm text-blue-600 hover:text-blue-800 font-medium">Create Account</a>
        </div>
        <a href="/" class="inline-block text-sm text-gray-500 hover:text-gray-700">
          ← Return to Homepage
        </a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white w-full">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 text-center text-gray-400">
      &copy; 2025 DoctorSync. All rights reserved.
    </div>
  </footer>

  <script src="/javascripts/loginHelp.js"></script>
</body>
</html>

