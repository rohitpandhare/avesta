<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DoctorSync</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="min-h-screen flex flex-col justify-between m-0" style="background-color: #000021;">
    <%- include('nav') -%>

    <main class="flex flex-row flex-grow justify-between items-start w-full px-4 py-4">
        <div id="videoContainer" class="w-1/3 lg:w-1/2 flex justify-start pr-2" style="height: 30%;">
            <video id="doctorSyncVideo" src="/images/doctorsync_vid.mp4" muted playsinline class=" rounded-lg shadow-lg" style="object-fit: cover;">
                Your browser does not support the video tag.
            </video>
        </div>


           <div id="mainContainer" style="margin-left: 80px;" class="w-full lg:w-1/2 flex flex-col justify-start lg:pl-4 mt-8 lg:mt-0">
            <div class="text-center">
                <h2 class="text-3xl md:text-4xl font-bold text-blue-800 mb-4 tracking-tight p-2 rounded-xl" style="border: 1px solid gold;">Welcome to DoctorSync</h2>
                <h4 class="text-xl md:text-2xl text-green-600 mb-8 font-semibold">Sync Your Health</h4>
                <p class="text-white text-lg max-w-3xl mx-auto leading-relaxed">
                    Revolutionize your healthcare experience with our integrated platform. Connect with medical
                    professionals and manage prescriptions in one secure place.
                </p>
            </div>

            <br> <br>
            <br>
            <div class="mb-8 lg:mb-20 mt-11">
                <h3 class="text-2xl md:text-3xl font-bold text-blue-800 mb-4 lg:mb-8 text-center">Why Choose DoctorSync?</h3>
                <div class="grid md:grid-rows-1 gap-4 lg:gap-8">
                    <div class="bg-white p-4 md:p-6 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-200">
                        <h4 class="text-lg md:text-xl font-semibold mb-2 md:mb-3 text-blue-600">Prescription Management</h4>
                        <p class="text-gray-600">Get Real Time Prescriptions from your doctor</p>
                    </div>
                    <div class="bg-white p-4 md:p-6 rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-200">
                        <h4 class="text-lg md:text-xl font-semibold mb-2 md:mb-3 text-blue-600">Remote Prescription View</h4>
                        <p class="text-gray-600">Get Generated Precriptions Just by its Prescription ID</p>
                    </div>
                </div>
            </div>
        </div>

    </main>
    
    <%- include('footer') -%>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const video = document.getElementById('doctorSyncVideo');
            const videoContainer = document.getElementById('videoContainer');
            let isHovering = false;
            let hasPlayedOnce = false;

            if (video && videoContainer) {
                videoContainer.addEventListener('mouseenter', () => {
                    isHovering = true;
                    if (video.paused) {
                        video.play();
                    }
                });

                videoContainer.addEventListener('mouseleave', () => {
                    isHovering = false;
                    if (!video.paused) {
                        video.currentTime = 0;
                    } else if (hasPlayedOnce) {
                        video.currentTime = 0;
                        hasPlayedOnce = false;
                    }
                });

                video.addEventListener('ended', () => {
                    hasPlayedOnce = true;
                    if (!isHovering) {
                        video.currentTime = 0;
                        hasPlayedOnce = false;
                    }
                });
            }
        });
    </script>
</body>

</html>